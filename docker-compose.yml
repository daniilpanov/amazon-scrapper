services:
    bsrstarter:
        build:
            context: ./bsr_starter_service
            target: bsrstarter
            dockerfile: Dockerfile
        restart: always
        networks:
            - net
    capsolver:
        build:
            context: ./captcha_service
            target: capsolver
            dockerfile: Dockerfile
        restart: always
        networks:
            - net
networks:
    net:
